import * as walletWebhookService from "../../walletWebhooks/service";
import { addOrRemoveAddresses } from "../../walletWebhooks/web3Provider";
import { Wallet } from "../types";

const upsertWebhook = async (wallet: Wallet) => {
  // const webhook = await walletWebhookService.createAndOrAttachWallet({});
};
